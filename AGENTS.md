# 全局 UI 设计规范

当任务涉及网页、桌面应用、移动端页面、组件或任何前端 UI 时，必须遵守以下规则。

## 1. 核心设计方向

默认采用：

- 视觉简约、克制、高级、现代、产品化。
- 优先通过字号、字重、间距、对齐和留白建立层级。
- 不依赖大量卡片、边框、渐变、光效、装饰字符或英文标签制造“高级感”。
- 页面首先要清晰、好读、好用，其次才是视觉表现。
- 设计应像成熟商业产品，而不是概念稿、展示稿或 AI 自动生成的模板。
- 不要为了让页面显得丰富而增加无实际作用的元素。

“简约”不等于所有内容都缩小，也不等于使用大量低对比度灰色小字。

## 2. 文案删减规则

页面中的每一段可见文字，至少必须完成以下一种任务：

1. 说明当前内容是什么。
2. 标记一个可执行操作。
3. 展示当前状态或进度。
4. 提供用户此刻必须知道的信息。
5. 展示错误、警告、权限或操作结果。

不符合以上条件的文字应删除。

必须主动删除以下类型的文案：

- 仅用于衬托氛围的文字。
- 重复按钮含义的说明。
- 用户通过界面已经能够看懂的操作提示。
- 为了填补空白而添加的副标题。
- 同一操作出现两遍或三遍的提示。
- 没有实际功能的英文眉题、BETA、版本号、宣传短语。
- 类似“按下麦克风开始”“点击按钮即可开始”“由易到难”等显而易见的文字。
- 按钮已经写明“开始录音”时，不要再在旁边写“点击麦克风后开始录音”。

一项操作原则上只保留一个清晰标签。

辅助说明只有在用户可能误解、操作存在风险或需要输入规则时才显示，并且：

- 最多一行。
- 使用直接、具体的语言。
- 优先在用户需要它的时刻出现，而不是永久占据页面。
- 首次使用说明优先采用一次性引导、Tooltip、Popover 或状态提示。

不得删除：

- 错误信息。
- 权限说明。
- 隐私和安全提示。
- 操作不可逆警告。
- 用户确实无法从界面理解的重要规则。
- 无障碍所需的 label、aria-label 和屏幕阅读器文本。

## 3. 字体与可读性

默认桌面端字号：

- 页面主标题：28–36px。
- 区块标题：20–24px。
- 主要正文：16–18px。
- 按钮文字：16–17px，medium 或 semibold。
- 表单标签：15–16px。
- 辅助文字：14–15px。
- 仅时间戳、次要元数据允许使用 12–13px。

禁止：

- 大量使用 10–12px 的灰色小字。
- 为了显得精致而故意降低文字可读性。
- 同一个卡片中出现过多字号层级。
- 使用过细字重承载正文。
- 通过全大写英文制造装饰感。

正文行高建议为 1.5–1.7。

中文界面优先保证中文字体清晰、字重稳定，不要为了风格使用难以阅读的特殊字体。

## 4. 信息层级

每个页面必须明确：

- 一个页面主目标。
- 一个最重要的主要操作。
- 当前任务或当前状态。
- 必要的次级操作。

每个区块最多使用三级视觉层级：

1. 标题或主要内容。
2. 主要操作或重要状态。
3. 必要的辅助信息。

不要在一个卡片中同时放置：

- 编号。
- 标题。
- 副标题。
- 标签。
- 状态。
- 说明。
- 提示。
- 装饰图标。

除非这些信息都确实有功能价值。

## 5. 布局与视觉语言

- 使用统一的 8px 间距体系，例如 8、12、16、24、32、48。
- 增大区块之间的留白，减少区块内部无意义文字。
- 避免卡片套卡片。
- 避免每一项内容都使用独立边框。
- 优先使用背景层级、间距和对齐分组。
- 每个页面最多设置一种主要强调色。
- 渐变、发光、玻璃效果和阴影只能少量使用。
- 不要同时使用金色描边、金色文字、金色阴影和金色渐变。
- 圆角应统一，通常使用 10–16px。
- 边框应该轻，不要让页面布满框线。
- 图标只在能够加快识别时使用，不能纯粹用于装饰。
- 不使用无意义的序号，例如 01、02、03，除非序号表示真实步骤或顺序。

## 6. 按钮与交互

- 一个区域只突出一个主要按钮。
- 主要按钮必须使用明确的动作名称，例如“开始跟读”，而不是模糊的“确认”。
- 图标按钮必须有 Tooltip 和 aria-label，但不一定永久显示说明文字。
- 不要在按钮旁边重复解释按钮功能。
- 状态变化通过按钮状态、进度、颜色或简短提示表达。
- 操作完成后优先给出结果反馈，而不是提前堆放大量教程文字。
- 复杂说明采用渐进式披露，需要时再展开。

## 7. 禁止出现的常见 AI UI 风格

除非用户明确要求，否则禁止：

- 为每个区块添加英文小标题。
- 添加没有功能的 “BETA”“PRO”“AI POWERED”。
- 大量渐变和霓虹发光。
- 页面中堆叠十几个圆角卡片。
- 每个标题下面都附带一行灰色说明。
- 为普通按钮添加冗长操作提示。
- 使用大量很小、很淡的字营造所谓精致感。
- 把简单功能设计成仪表盘。
- 用装饰性数据、编号和状态填充空白。
- 为了对称而增加无意义内容。

## 8. 实施工作流

进行 UI 修改时，必须按以下顺序执行：

1. 阅读现有页面和相关组件。
2. 识别页面的核心任务。
3. 审查所有可见文案。
4. 删除重复、显而易见或纯装饰文案。
5. 合并重复区块和重复操作。
6. 重新建立字体、间距和视觉层级。
7. 再进行颜色、边框、圆角和动效优化。
8. 实际修改代码，而不是只给设计建议。
9. 运行项目，检查页面是否正常。
10. 检查桌面端和窄屏下是否存在溢出、过小字体或布局问题。

修改完成后说明：

- 删除了哪些无效文案。
- 合并了哪些重复元素。
- 调整了哪些字号和间距。
- 保留了哪些必要提示及保留原因。

## 9. 默认决策原则

当无法确定是否应该添加一段文字或一个元素时：

- 默认不添加。
- 优先删除，而不是增加。
- 优先留白，而不是填满。
- 优先放大重要内容，而不是缩小所有内容。
- 优先让用户直接操作，而不是先阅读说明。
- 优先保持现有设计系统一致，而不是重新发明一套风格。